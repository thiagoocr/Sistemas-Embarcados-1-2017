#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>

int main(int argc, char **argv){
	int i, qtde;
	qtde = argc;
	for(i = 0; i < argc; i++){ //desenvolve a qtde de processos necessarios
		pid_t pid[i]; 
	}
	for(i = 0; i < argc; ){

	}
	
		

	return 0;
}
